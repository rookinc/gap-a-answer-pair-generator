\section{Work plan}

The next audit sequence should proceed in three stages.

\subsection{Stage 1: Rebuild the row-pair universe}

The first task is to reproduce the 48-pair candidate universe from the copied Project 18 artifacts and record it as the baseline candidate universe.

\subsection{Stage 2: Expand candidate supply}

The second task is to enlarge or regenerate the candidate universe using source-native constraints. Candidate sources include station provenance fields, role-pair channels, \(C\)-transition support, signed \(G_{15}\) structure, and any available \(G_{60}\)-native quotient data.

\subsection{Stage 3: Admission-blind selection}

The final task is to apply the reciprocal answer-pair grammar without using realized/admitted labels. The selector succeeds only if it recovers the 12 expected pairs exactly.

The project should preserve negative results. A failed generator family is useful if its boundary is explicit.
