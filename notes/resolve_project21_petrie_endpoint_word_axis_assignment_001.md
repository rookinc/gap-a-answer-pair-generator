# Resolve Project 21 Petrie endpoint-word axis assignment 001

## Status

PROJECT21_PETRIE_ENDPOINT_WORD_AXIS_ASSIGNMENT_RESOLVED

## Resolved mapping

- within axis = endpoint polarity
- class axis = FV versus VF word order
- product axis = endpoint polarity XOR FV/VF word order

## Reason

The paper-defined chamber construction applies each transport word to both endpoint representatives. Endpoint polarity therefore varies the representative within a fixed word branch. Swapping FV and VF changes the branch family. The Project 21 within generator uniquely preserves alternate-image class, while the class generator exchanges it.

## Boundary

- Finite axis-assignment theorem under explicit manuscript premises.
- Endpoint polarity is assigned to the class-preserving within-axis.
- FV versus VF word order is assigned to the class-exchanging axis.
- The result uses the paper-defined chamber construction, not recovered executable generator provenance.
- No Petrie-parity shadow, face-chirality theorem, literal {5,3,4}, Higgs mechanism, physics, or Gap A closure claim.

## Next

`audit_project21_petrie_endpoint_word_mapping_boundary_001`
